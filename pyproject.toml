[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "makervox-publish"
version = "0.1.1"
description = "Self-hosted social publishing for people who bring their own developer apps."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Fred Taylor" }]
keywords = ["publishing", "tiktok", "instagram", "facebook", "social", "automation"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Internet",
  "Typing :: Typed",
]

# ONE runtime dependency. Everything else is an extra, and importing makervox_publish
# must never import any of them.
dependencies = ["requests>=2.28"]

[project.optional-dependencies]
# TOML config on Python < 3.11 (3.11+ uses the stdlib tomllib).
toml = ["tomli>=2.0; python_version < '3.11'"]
# YAML config is optional; JSON and TOML need nothing extra.
yaml = ["PyYAML>=6.0"]
gcp = ["google-cloud-secret-manager>=2.16", "google-cloud-storage>=2.10"]
s3 = ["boto3>=1.28"]
keyring = ["keyring>=24.0"]
# X posts are signed with OAuth 1.0a user context, not an OAuth 2.0 bearer
# token, so it needs a signer. Without this extra the X client is inert and says
# so; it never raises ImportError at import time.
x = ["requests-oauthlib>=1.3"]
dev = [
  "pytest>=7.4",
  "pytest-cov>=4.1",
  "mypy>=1.6",
  "ruff>=0.1",
  "tomli>=2.0; python_version < '3.11'",
  "PyYAML>=6.0",
]

[project.urls]
Homepage = "https://github.com/example-org/makervox_publish"
Documentation = "https://github.com/example-org/makervox_publish/tree/main/docs"
Issues = "https://github.com/example-org/makervox_publish/issues"

[project.scripts]
makervox-publish = "makervox_publish.cli.main:main"

# Third parties publish providers/stagers against these groups so that makervox_publish
# never takes a dependency on Vault, AWS, 1Password, etc.
[project.entry-points."makervox_publish.credential_providers"]
env = "makervox_publish.credentials.env:EnvProvider"
static = "makervox_publish.credentials.static:StaticProvider"
json_file = "makervox_publish.credentials.json_file:JsonFileProvider"
keyring = "makervox_publish.credentials.keyring:KeyringProvider"
gcp_secret_manager = "makervox_publish.credentials.gcp_secret_manager:SecretManagerProvider"

[project.entry-points."makervox_publish.cover_pickers"]
brightness_scan = "makervox_publish.media.cover:BrightnessScanCoverPicker"

[project.entry-points."makervox_publish.token_stores"]
file = "makervox_publish.state.token_store:FileTokenStore"

# Instagram can only PULL media by URL, so a stager is required to publish a
# local file. There is deliberately no default: only you know where you can
# serve a file from.
[project.entry-points."makervox_publish.media_stagers"]
gcs = "makervox_publish.platforms.meta.staging.gcs:GcsStager"
static_dir = "makervox_publish.platforms.meta.staging.http_dir:StaticDirStager"

[project.entry-points."makervox_publish.link_taggers"]
noop = "makervox_publish.text.links:NoopTagger"
utm = "makervox_publish.text.links:UtmTagger"

# Counter stores back the pay-per-post daily cap. The JSON one is an unlocked
# read-modify-write and is correct only for a single serialized writer; the
# SQLite one is safe for concurrent callers. See makervox_publish.state.counters.
[project.entry-points."makervox_publish.counter_stores"]
json = "makervox_publish.state.counters:JsonCounterStore"
sqlite = "makervox_publish.state.counters:SqliteCounterStore"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
makervox_publish = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error"]

[tool.ruff]
line-length = 100
target-version = "py39"

# An implicit rule set is not reproducible: with no `select`, a newer ruff
# enables rules this code has never been checked against, and CI turns red on a
# day nobody changed anything. It did — 1226 findings on the first public push,
# against a local run that passed clean. Name the rules, and pin the version in
# CI to match.
[tool.ruff.lint]
# This is ruff's own default set, stated explicitly. Naming it is the point:
# the previous config selected nothing, so the rule set was whatever the
# installed ruff happened to default to that week.
#
# Deliberately NOT adding UP/B/C4/SIM yet. They produce ~1100 findings on this
# code, and suppressing that many at once would make the lint meaningless.
# Adopting one rule family at a time, actually fixing it, is the honest path.
select = ["E4", "E7", "E9", "F"]
ignore = [
    "E501",  # long lines are a formatter concern, not a lint failure
]

[tool.ruff.lint.per-file-ignores]
# __init__ re-exports the public API; unused-import IS the point.
"src/makervox_publish/__init__.py" = ["F401"]
"src/makervox_publish/*/__init__.py" = ["F401"]
# Tests reach into internals and shadow names on purpose.
"tests/*" = ["B011", "SIM117"]

[tool.mypy]
python_version = "3.9"
warn_unused_ignores = true
disallow_untyped_defs = false
